---
name: build-test-push
description: Build, test, and push workflow for {{PROJECT_NAME:project name}}.
foundation: build-test-push
foundationVersion: ">=1.0.0"
---

# Build Test Push — {{PROJECT_NAME:project name}} Adapter

Load the foundation:

```
Read: .apex/foundation/build-test-push/SKILL.md
```

## Build and test commands for this project

Tech stack: {{FRAMEWORK:detected frameworks}}

TODO: Fill in the exact commands for this repo:

**TypeScript compile:**
```bash
TODO: e.g. npx tsc --noEmit
```

**Lint:**
```bash
TODO: e.g. npm run lint:check
```

**Build:**
```bash
TODO: e.g. npm run build
```

**Test:**
```bash
TODO: e.g. npm test
```

## Commit conventions

TODO: Document the commit message format (e.g. Conventional Commits: `feat(scope): description`).

## Default base branch

TODO: Specify the default base branch (e.g. `main`, `development`).
